import { FC } from 'react';
const App: FC = () => {
  return (
    <div>
      <input type="text" className="input" />
      <button type="button" className="button">ABC</button>
    </div>
  );
};

export default App;
