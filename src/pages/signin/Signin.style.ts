import styled from 'styled-components';
import { ButtonBase, InputBase } from '../../core/styles/base.style';

export const InputSign = styled(InputBase)``;

export const ButtonSign = styled(ButtonBase)`
  margin: 0.5rem;
`;

export const SignContainer = styled.div`
  position: relative;
  padding: 1rem 1rem;
`;

export const SignChild = styled.div`
  max-width: 350px;
  margin: auto;
`;
